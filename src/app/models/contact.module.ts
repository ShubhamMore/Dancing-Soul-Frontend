export class ContactModel {
  constructor(
    // tslint:disable-next-line: variable-name
    public _id: string,
    public address: string,
    public email: string,
    public phone: string
  ) {}
}
